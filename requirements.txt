# ═══════════════════════════════════════════════════════════════
# Deep Paper Reader — Requirements
#
# LOCAL models:  Qwen3.5-27B Q8 (VLM, ~29GB), MinerU, BGE-M3
# API models:    qwen/qwen3.6-plus:free via OpenRouter ($0)
# ═══════════════════════════════════════════════════════════════

# ─── Core ───────────────────────────────────────────────────────
pydantic>=2.0
pyyaml>=6.0
rich>=13.0

# ─── PDF Parsing: MinerU ────────────────────────────────────────
# MinerU 내장: DocLayout-YOLO, UniMERNet, PaddleOCR, StructEqTable
# GPU ~6GB / CPU 모드 가능
magic-pdf[full]
pymupdf>=1.24.0
Pillow>=10.0

# ─── Local VLM: Qwen3.5-27B (via Ollama or MLX) ────────────────
# Ollama가 모델 서빙 — Python에서 HTTP client로 호출
# ollama pull qwen3.5:27b-q8_0  (~29GB, 32GB 할당으로 실행)
# 대안: pip install mlx-vlm (Apple Silicon 최적화, ~20-30% 빠름)
httpx>=0.27.0
ollama>=0.4.0

# ─── Local Embedding: BGE-M3 ───────────────────────────────────
# ~2GB | dense + sparse 하이브리드
FlagEmbedding>=1.2.0
sentence-transformers>=3.0.0
torch>=2.1.0

# ─── API Client: OpenRouter (qwen3.6-plus:free) ────────────────
# 추론 전체: 논증/가설/예측/토론/리뷰 — $0/M tokens
# OpenAI-compatible → openai 패키지 사용
openai>=1.50.0

# ─── API Client: Gemini Flash (free fallback) ──────────────────
google-genai>=1.0.0

# ─── Zotero ────────────────────────────────────────────────────
pyzotero>=1.5.0

# ─── Feedback Web UI ───────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0

# ─── Database ──────────────────────────────────────────────────
sqlalchemy>=2.0
